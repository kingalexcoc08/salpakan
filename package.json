{
  "name": "salpakan",
  "private": true,
  "version": "0.1.0",
  "description": "Salpakan (Game of the Generals) Digital Arbiter — MVP monorepo",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "test": "npm run test --workspace=packages/shared && npm run test --workspace=packages/server",
    "test:shared": "npm run test --workspace=packages/shared",
    "test:server": "npm run test --workspace=packages/server",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/server && npm run build --workspace=packages/web",
    "dev:server": "npm run dev --workspace=packages/server",
    "dev:web": "npm run dev --workspace=packages/web"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
