# Leave unset to call the API at a relative "/api" path (works with the Vite
# dev proxy, and with any deployment where the web app and API share an origin).
# Set to a full origin (e.g. https://api.example.com) when the API is hosted separately.
VITE_API_BASE_URL=
